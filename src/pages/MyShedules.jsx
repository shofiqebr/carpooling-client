import { Helmet } from "react-helmet";

const MyShedules = () => {
    return (
        <div>
             <Helmet>
      <title>My Shedules</title>
      </Helmet>
            msh
        </div>
    );
};

export default MyShedules;